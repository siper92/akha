version: "2"
sql:
  - engine: "sqlite"
    queries: "_defs/db/q.*.sql"
    schema: "_defs/db/schema.sql"
    gen:
      go:
        package: "db_sdk"
        out: "sdk/db-sdk"
        emit_interface: true
        emit_json_tags: true
